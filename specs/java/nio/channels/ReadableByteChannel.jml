package java.nio.channels;

public interface ReadableByteChannel extends Channel {
    // FIXME - needs to model the buffer of content
    
    //@ public behavior
    //@   assignable b.hb[b.position .. b.position -1], b.position;
    //@   ensures \result >= -1;
    //@   ensures \result >= 0 ==> b.position == \old(b.position) + \result;
    public abstract int read(java.nio.ByteBuffer b) throws java.io.IOException;
}
